

plugins {
    id 'java'
    id "idea"
    id 'java-library'
    id "maven-publish"
    id "com.github.johnrengelman.shadow" version "7.0.0"
}

apply plugin: 'maven-publish'

group = 'io.github.divios'
def pluginVersion = "1.3"
version = pluginVersion

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

repositories {
    mavenCentral()
    mavenLocal()

    maven {
        name = 'spigotmc-repo'
        url = 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/'
    }
    maven {
        name = 'sonatype'
        url = 'https://oss.sonatype.org/content/groups/public/'
    }

    maven {
        name = 'codemc-snapshots'
        url = 'https://repo.codemc.io/repository/maven-snapshots/'
    }

    maven {
        name = 'codemc-repo'
        url = 'https://repo.codemc.org/repository/maven-public/'
    }

    maven { url 'https://jitpack.io' }

    maven { url "https://repo.dmulloy2.net/repository/public/" }

    maven {
        url repsyUrl
        credentials {
            username repsyUsername
            password repsyPassword
        }
    }

}

task updateJars {
    doLast {
        copy {
            from 'build/libs/Wards-' + pluginVersion + "-all.jar"
            into 'C:/Users/serbe/Desktop/server_pruebas/1.17/plugins'
            rename ("Wards-" + pluginVersion + "-all.jar"
                    , "Wards-" + pluginVersion + ".jar")
        }
        copy {
            from 'build/libs/Wards-' + pluginVersion + "-all.jar"
            into 'C:/Users/serbe/Desktop/server_pruebas/1.16/plugins'
            rename ("Wards-" + pluginVersion + "-all.jar"
                    , "Wards-" + pluginVersion + ".jar")
        }
        copy {
            from 'build/libs/Wards-' + pluginVersion + "-all.jar"
            into 'C:/Users/serbe/Desktop/server_pruebas/1.12/plugins'
            rename ("Wards-" + pluginVersion + "-all.jar"
                    , "Wards-" + pluginVersion + ".jar")
        }
        copy {
            from 'build/libs/Wards-' + pluginVersion + "-all.jar"
            into 'C:/Users/serbe/Desktop/server_pruebas/1.8/plugins'
            rename ("Wards-" + pluginVersion + "-all.jar"
                    , "Wards-" + pluginVersion + ".jar")
        }
    }
}

shadowJar {
    relocate('org.bstats', 'io.github.divios.wards.shaded.bstats')
    relocate "de.tr7zw.nbtapi", "io.github.divios.wards.shaded.nbtApi"
    relocate('io.github.divios.core_lib', 'io.github.divios.wards.shaded.Core_lib')
    relocate('com.cryptomorin.xseries', 'io.github.divios.wards.shaded.XSeries')

    minimize()
}

dependencies {
    compileOnly 'org.spigotmc:spigot-api:1.17-R0.1-SNAPSHOT'
    compileOnly group: "com.comphenix.protocol", name: "ProtocolLib", version: "4.7.0-SNAPSHOT"
    //implementation 'org.ehcache:ehcache:3.8.1'

    implementation 'io.github.divios:core_lib:2.0'
}

processResources {
    expand projectVersion: pluginVersion

}

task deploy(dependsOn: ["build", "updateJars"]) {
    updateJars.mustRunAfter jar
}

compileJava.dependsOn clean
build.dependsOn shadowJar
